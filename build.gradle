plugins {
    id 'java'
}

allprojects {
    group 'com.playko'
    version '0.0.1-SNAPSHOT'
}

subprojects {
    repositories {
        mavenCentral()
    }

    dependencies {
        // Dependencias comunes
    }
}
